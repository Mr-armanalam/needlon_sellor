import { Button } from "@/components/ui/button";

const CTAButton = () => {
  return <Button className={'bg-rose-500 hover:bg-rose-600 text-base cursor-pointer text-white'}>Start Selling Free</Button>;
};

export default CTAButton;
