"use client";

interface Props {
    setSaveStatus: (
        status:
            | "Saved ✓"
            | "Saving..."
            | "Changes pending",
    ) => void;
}

export default function BankAndPayoutSection(
    {}: Props,
) {
    return <div>arman</div>
}
